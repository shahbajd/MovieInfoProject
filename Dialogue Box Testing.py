from Tkinter import *#import tkFileDialog as filedialogimport osclass Dialogue_Box:    def __init__(self):        root.minsize(width=350, height=150)        root.maxsize(width=350, height=150)        root.configure(background='#ECECEC')        root.title('Movie Info Retreival Project') # Title of Tkinter Window        main_frame = Frame() # Create a Master Frame        main_frame.pack()        main_frame.configure(background='#ECECEC') # Apple Grey background                self.heading_message = Label(main_frame, text='Retreival Complete!', background='green')        self.heading_message.pack(padx=5, pady=15)                self.another = Label(main_frame, text='Search Another Title?', padx=0, pady=0, background='#ECECEC')        self.another.pack(padx=10, pady=10)                self.no = Button(main_frame, text="QUIT", command=self.Quit)        self.no.pack(pady=5, padx=5, side=LEFT)                self.yes = Button(main_frame, text="YES")        self.yes.bind('<Button-1>', self.Another)        self.yes.pack(pady=5, padx=5)        root.bind('<Return>', self.Another)        def Another(self, event):        self.another = True        root.destroy()                def Quit(self):        self.another = False        root.destroy()                                # tie ENTER and ok click together                        # master.minsize(width=400, height=400)        # master.maxsize(width=400, height=400)                # Label(text="Enter Search Term Below", background="green").grid(columnspan=2, row=0, column=0, padx=0, pady=0)        # Label(text="TITLE:", background="red").grid(row=1, column=0, padx=0, pady=0)        # query = Entry(master)        # query.grid(row=1, column=1, padx=0, pady=0)root = Tk()#root.withdraw()os.system('''/usr/bin/osascript -e 'tell app "Finder" to set frontmost of process "Python" to true' ''')startup = Dialogue_Box()root.mainloop()print startup        # redbutton = Button(frame, text="Red", fg="red")        # redbutton.pack( side = LEFT)#os.system('''/usr/bin/osascript -e 'tell app "Finder" to set frontmost of process "Python" to true' ''')#file_path = filedialog.askopenfilename() #Obtain File Path#print file_path#root.destroy()